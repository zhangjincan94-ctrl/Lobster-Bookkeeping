# 查询统计优化（2026-09-22）

入口：底部「查询」Tab。

## 已实现
- 将全量账单读取从页面中拆到 `miniprogram/services/ledger.js`；校验分页完整性，失败不返回部分汇总。
- 修复仅汇总前100笔账单的问题，分别展示出货账额和进货账额。
- 使用请求序号隔离旧筛选响应，离开页面后停止后续分页。
- 新增近7天、本月快捷日期，拦截倒置日期范围。
- 单次渲染30笔，触底或点击继续查看追加；汇总仍覆盖全部匹配账单。
- 补齐加载、失败重试、空结果状态；下拉刷新等待请求结束。
- 保留既有深青色风格，扩大筛选触控区，长金额换行，客户名称溢出省略。

## 网络参考
已阅读微信官方 WeUI：https://weui.io/ 。采用其原生体验一致性与操作反馈思路，未引入第三方依赖或复制同类产品素材。

## 验证
- `cd server; npm.cmd test`：已有22项后端测试通过。
- 根目录运行 `node --test miniprogram/tests/query.test.js`：6项查询回归测试通过。
- 新增及修改的JavaScript通过 `node --check`；`git diff --check`通过。
- 尚未在微信开发者工具或真机完成视觉、触控和真实接口联调；自动化使用模拟接口，不访问线上账务数据。

## 后续建议
1. 大数据量场景增加后端聚合接口和分页查询，避免为统计拉取全部明细。
2. 对商品库和客户列表统一检查分页完整性与失败反馈。
3. 通用账本与旧龙虾业务的页面、路由分包整理，在确认旧业务使用情况后实施。
