# 龙虾生意账 Stitch UI 设计需求文档（多商品账本版）

## 1. 文档目的

本文件用于让 Stitch 为“龙虾生意账”生成完整的微信小程序移动端 UI。请依据本文件设计高保真页面、组件与状态；视觉风格必须保持“未来经营控制台”方向，业务模型改为统一客户、多商品账单和客户往来账管理。

本文件定义的是我们自己的原创产品规格。可参考同类产品的通用业务模式，但不得使用第三方产品名称、Logo、截图素材、文案或源码。

产品定位：面向个体经营者的轻量化开单、客户往来账、商品库、回款与对账分享工具。

核心任务：用户维护自己的商品库和商品分类；选择客户并开具进货或出货账单；一张账单可以包含多个商品；查看客户账本、记录客户回款、生成只读结账信息并分享。

主要用户：需要频繁用手机开单、查账、催款、对账的小型经营者。用户处于门店、仓库或配送场景，界面必须适合单手操作、快速输入、清晰查看金额。

## 2. 已确认的业务范围

### 2.1 已确认功能

- 使用统一的“客户”模型，不再在 UI 上分成买家与供应商两个独立模块。
- 支持“进货”和“出货”账单。
- 一张账单支持多个商品明细行。
- 商品和分类由用户自己维护；不提供固定龙虾规格或固定品种列表。
- 开单时从商品库选择商品，可新增商品后再选入账单。
- 增加客户回款入口，保留回款记录并展示回款前后余额。
- 客户账本可以按日期范围筛选，生成只读的结账信息并分享。
- “一键生成结账信息”只生成可分享的对账内容，不自动修改账单、回款或结清状态。

### 2.2 首版不做

- 多人协作、角色权限、成员邀请。
- 手机号绑定、充值中心、会员订阅、在线支付。
- 金融借贷、资金托管、银行卡或支付通道。
- AI 智能录入、智能排序、文件导出、退货/红冲单，除非后续明确确认。

### 2.3 待确认，设计中不可擅自定死

1. 客户能否同时存在进货和出货往来，或只能选择单一类型。
2. 一张账单是否整单统一为进货或出货，或允许同单混合方向。
3. 商品字段是否需要自定义单位、默认单价、备注。
4. 客户回款是否自动核销最早未结账单，或由用户手工选择要核销的账单。
5. 结账分享是仅小程序只读链接，还是还要图片、可复制文字。

在未确认前，Stitch 应将上述内容设计为可扩展状态：客户类型可使用可选控件占位；商品详情预留单位、默认单价、备注区域；回款页预留“核销明细”区域；分享页预留多种分享入口，但不可展示不存在的支付功能。

## 3. 平台与交付要求

### 3.1 平台

- 微信小程序移动端，不是桌面后台或营销官网。
- 基准画板：375 x 812 px，兼容 320 至 430 px 宽度。
- 采用微信小程序常见的顶部导航、底部 Tab Bar、选择器、弹窗、Toast、分享卡片交互。
- 所有页面都要设计正常数据、加载、空数据、提交中、错误、禁用和删除确认状态。

### 3.2 Stitch 需要交付

1. 本文“页面清单”中的所有页面与弹窗。
2. 可复用组件：顶部栏、底部导航、未来数据面板、状态标签、筛选器、账单列表项、商品明细表、表单行、金额摘要、固定底部操作栏、空状态、弹窗与分享卡片。
3. 颜色、字号、间距、圆角、边框、阴影和状态色标注。
4. 完整流程：开单 -> 查看账单 -> 客户账本 -> 客户回款 -> 分享回款单；客户账本 -> 日期筛选 -> 生成结账信息 -> 分享只读账单；商品库 -> 添加商品 -> 回到开单选择商品。
5. 所有按钮、字段、筛选项和状态都应有明确命名，便于后续映射到 WXML/WXSS/JS。

## 4. 视觉风格：未来经营控制台（保持不变）

### 4.1 视觉原则

专业、可靠、利落，带有“未来经营控制台”的科技感。它是每天用来开单和管理往来账的工具，应像清晰的经营数据终端：高对比数字、明确状态、克制冷色光效和少量珊瑚红操作色。

不要做成游戏 HUD、紫色霓虹、赛博朋克海报、金融投机产品或营销主页。

### 4.2 设计令牌

| 用途 | 色值 | 说明 |
| --- | --- | --- |
| 应用底色 | `#0B1617` | 石墨黑偏青，不使用深蓝。 |
| 表面层 | `#122526` | 页面面板、导航、弹窗。 |
| 主色 | `#00A9A5` | 主按钮、已选状态。 |
| 冷青高光 | `#5DE6DC` | 数据光标、边线、图表、关键数字的细线高光。 |
| 出货/收入 | `#FF6246` | 出货主操作、收入强调。 |
| 进货/成本 | `#E7B447` | 进货、成本、待处理提醒。 |
| 成功 | `#4BCE91` | 回款、已结清、完成。 |
| 风险 | `#FF6868` | 欠款、删除、危险操作。 |
| 主文字 | `#F2F7F5` | 标题、金额、主要信息。 |
| 次文字 | `#A1B2B0` | 日期、单位、说明。 |
| 分割线 | `#294142` | 信息区块和表格边界。 |

### 4.3 排版与布局

- 系统无衬线字体；金额和数量采用等宽数字风格。
- 页面标题 20 px，区块标题 16 px，正文 14 px，辅助文字 12 px；关键金额 24 至 30 px。
- 页面左右边距 16 px，普通区块间距 12 至 16 px，最小触控高度 48 px。
- 数据面板圆角 4 至 8 px，使用 1 px 低对比边线和极轻阴影；不使用卡片套卡片。
- 标题区和图表可使用极弱点阵/网格刻度；禁止粒子、光球、宇宙背景、大面积渐变。
- 文字必须适配金额、长客户名和长商品名；不允许重叠或被按钮遮挡。

### 4.4 禁止项

- 不使用营销 Hero、超大标题、玻璃拟态、紫色霓虹、蓝紫渐变、粒子特效、游戏血条或过度圆角。
- 不使用图片代替真实表单、表格、按钮或动态金额。
- 不新增在线支付、借贷、会员、广告、充值或协作入口。

## 5. 信息架构

### 5.1 底部导航

| Tab | 页面 | 核心内容 |
| --- | --- | --- |
| 开单 | 多商品账单 | 快速选择客户和商品，录入进货或出货。 |
| 总账本 | 账单总览 | 按年月、日期查看全部账单。 |
| 客户 | 客户管理 | 搜索客户、查看余额、进入客户账本。 |
| 查询统计 | 流水查询与统计 | 按客户、商品、日期、金额查询和汇总。 |
| 我的 | 店铺与基础设置 | 店铺资料、商品库、分类管理、帮助、退出。 |

### 5.2 页面关系

```text
开单
  -> 选择客户 / 新增客户
  -> 商品库 -> 选择商品 / 新增商品
  -> 提交账单 -> 账单详情

总账本
  -> 日期分组 -> 账单详情 -> 查看 / 修改 / 删除

客户
  -> 客户资料 -> 客户账本
                  -> 客户回款 -> 回款单分享
                  -> 生成结账信息 -> 只读结账分享页

查询统计
  -> 流水筛选 -> 账单详情
  -> 经营统计

我的
  -> 店铺资料
  -> 商品库 / 分类管理
```

## 6. 核心业务与交互规则

### 6.1 多商品账单

- 账单头至少包含：交易日期、客户、账单方向（进货或出货）、备注。
- 账单明细行至少包含：商品、数量、单价、小计；总金额为所有小计之和。
- 用户可添加、移除和清空商品明细行。
- 商品必须从商品库选择；商品库为空时应给出“新增商品”的明确入口。
- 商品名称、数量、单价和小计在小屏上必须可扫描，不能因未来风格牺牲表格可读性。

### 6.2 统一客户与余额

- 客户列表展示名称、往来方向标签、当前余额和进入账本入口。
- 客户账本按日期范围展示账单、回款、账额、回款总额与当前余额。
- 进货与出货的金额方向必须在视觉和文案上清晰区分，不能只依赖颜色。
- 客户资料可以维护名称、电话、会计起始日；客户类型规则等待确认。

### 6.3 客户回款

- 独立页面录入客户、回款日期、回款金额；可查看历史回款。
- 提交前展示回款前余额；提交成功后展示回款后余额并生成回款单。
- 回款单为只读分享内容，显示客户、日期、金额、前欠与现欠。
- 核销规则等待确认，设计上预留“核销明细”区域，但不要假装已实现自动核销。

### 6.4 结账信息与分享

- 客户账本可选择开始和结束日期，生成该期间的账单、回款、应结余额摘要。
- 生成结账信息不改变账单状态、不自动清零余额、不生成支付请求。
- 分享页只读，显示客户、期间、明细、账额、回款与待结余额；不能显示内部编辑、删除、商品成本或管理入口。

### 6.5 商品库与分类

- 商品由用户新增、编辑、删除、分类；没有任何固定品种。
- 商品库包含全部和分类 Tab、空状态、搜索/刷新入口、选中数量状态。
- 分类管理可以使用独立页面或底部弹窗；支持新增、重命名、删除分类。
- 商品字段细节等待确认，但 UI 需为名称、分类、单位、默认单价、备注预留位置。

## 7. 页面清单与按钮

### P01 开单 / 记账

用途：创建一张进货或出货的多商品账单。

页面区块：

1. 交易日期：默认当天，可选择“今天”“昨天”或自定义日期。
2. 交易客户：显示当前客户，支持搜索选择、新增客户和留空规则（待确认）。
3. 账单方向：进货/出货选择器；若后续确认整单统一，使用明显分段控制。
4. 商品明细表：商品、数量、单价、小计、移除。支持多行。
5. 操作区：总金额、备注、设置入口。
6. 固定底部确认按钮。

按钮与操作：今天、昨天、选择客户、添加商品、商品库、删除商品行、清空明细、备注、设置、确认、进入客户回款。

状态：商品库为空、未选客户、无商品明细、金额为零、保存中、保存成功、保存失败。

### P02 商品选择弹窗

用途：从商品库选择一个或多个商品加入当前账单。

页面区块：搜索框、分类筛选、商品列表、已选数量、确认操作。

按钮与操作：搜索、切换分类、选择/取消选择、确认加入、取消、新增商品。

### P03 商品库

用途：维护用户自己的商品和分类。

页面区块：工具栏、全部/分类 Tab、商品列表、已选数量、空状态。

按钮与操作：添加商品、刷新、分类管理、分类 Tab、搜索、编辑商品、删除商品、批量选择（是否启用待确认）。

空状态：没有商品时显示“先添加一个商品”，提供添加商品按钮。

### P04 新增/编辑商品

用途：维护单个商品的基础资料。

字段：商品名称、分类、单位、默认单价、备注。其中单位、默认单价、备注是否必填等待确认。

按钮与操作：保存、删除商品（编辑状态）、取消、管理分类。

### P05 总账本

用途：查看全部账单，按年月和日期分组。

页面区块：年月汇总条、日期汇总条、账单卡片、展开商品明细、分页/没有更多状态。

每张账单显示：客户名称、进货/出货方向、总金额、日期、商品数量、展开后的商品/数量/单价/小计。

按钮与操作：展开/收起、查看详情、修改账单、删除账单、日期筛选。修改和删除必须二次确认。

### P06 客户管理

用途：统一维护所有往来客户并快速查看余额。

页面区块：搜索、快捷工具、客户方向筛选、客户列表、总余额摘要。

按钮与操作：搜索、添加客户、刷新、智能排序（暂不实现则不设计为可点击）、全部/出货/进货筛选、进入账本、进入客户资料。

客户行显示：名称、方向标签、当前余额、账本按钮、资料按钮。

### P07 新增/编辑客户

用途：维护客户资料与账务起点。

字段：客户名称、客户类型、会计起始日、电话。

按钮与操作：保存、删除或归档、选择客户类型、选择会计起始日、拨打电话。

注意：删除已有账单的客户不能直接删除历史数据。设计应展示“归档”或明确的失败提示状态，具体规则等待确认。

### P08 客户账本

用途：查看某个客户在一个日期范围内的账单、回款和余额。

页面区块：客户标题、开始日期/结束日期筛选、分享按钮、账单与回款汇总、待结余额、账单列表。

按钮与操作：日期筛选、清除日期、分享、生成结账信息、添加客户回款、查看账单、修改账单、删除账单、查看回款记录。

汇总必须清晰显示：账单总额、回款总额、待结余额；金额方向同时用文字和状态色表达。

### P09 客户回款

用途：录入一笔客户回款并显示余额变化。

字段：回款客户、回款日期、回款金额、回款方式、备注。后两项是否必填等待确认。

页面区块：回款记录入口、回款前余额、回款后余额预测、核销明细占位区、提交按钮。

按钮与操作：选择客户、查看记录、提交、取消、分享回款单。

### P10 回款单分享页

用途：给客户查看某一笔回款的只读凭证。

显示：标题、客户、回款日期、回款金额、回款前余额、回款后余额。

按钮与操作：分享、返回。不能编辑或删除。

### P11 结账信息分享页

用途：给客户查看指定日期范围的只读对账单。

显示：客户、账期、账单条数、账单总额、回款总额、待结余额、账单明细、生成时间。

按钮与操作：分享、返回、展开明细。不能编辑、删除或支付。

### P12 交易流水查询

用途：按条件检索所有账单并汇总数量与金额。

筛选：进货/出货、客户名称、商品名称、严格匹配、开始日期、结束日期、最低价格、最高价格。

按钮与操作：统计数量与金额、清空筛选、分页、查看详情、分析、导出、更多。

注意：分析、导出、更多首版不实现时必须显示为“即将支持”或从界面移除，不能做成无响应按钮。

### P13 经营统计

用途：查看经营趋势、进出货金额、客户待结余额和商品排行。

页面区块：日期范围、进货/出货/回款/待结摘要、趋势图、商品排行、客户余额排行。

按钮与操作：切换时间范围、切换趋势维度、进入客户账本、进入商品详情。

### P14 我的 / 基础设置

用途：维护店铺基本资料和访问商品库、分类管理。

页面区块：店铺头像与名称、联系电话、商品库、分类管理、帮助、退出登录。

按钮与操作：编辑店铺名称、编辑电话、进入商品库、进入分类管理、帮助、退出登录。

不显示：多人协作、手机号绑定、充值中心、会员、支付入口。

## 8. 通用组件与状态

### 8.1 金额和方向

- 人民币使用 `¥` 和两位小数，例如 `¥1,280.00`。
- 出货/收入使用珊瑚红强调，进货/成本使用金色强调，回款/已结清使用绿色；同时必须搭配文字，不可只依赖颜色。
- 当前余额必须显示方向文本，例如“待收”“待付”“余额”。

### 8.2 表格与列表

- 多商品账单表在 375 px 宽度下优先显示商品、数量、单价、小计；名称过长省略但可进入详情查看。
- 账单列表采用紧凑信息带，不使用瀑布流大卡片。
- 列表必须包含加载、空状态、重试、没有更多数据和删除确认状态。

### 8.3 弹窗与危险操作

- 新增客户、商品选择、分类管理、回款、删除确认使用统一弹窗组件。
- 删除账单、删除商品、删除客户均需要二次确认。
- 保存中禁用重复提交；错误信息展示在对应字段附近，不只使用短暂 Toast。

## 9. Stitch 设计约束

1. 保持五个底部 Tab：开单、总账本、客户、查询统计、我的。
2. 开单页必须支持多商品明细，不再设计固定龙虾规格、斤数或采购货源分摊。
3. 商品必须是用户维护的自定义数据，分类也由用户维护。
4. 客户账本、回款、结账分享是核心流程，不能隐藏在二级菜单深处。
5. 分享页面必须与商家内部页面明显区分，且完全只读。
6. 不使用营销 Hero、超大标题、紫色霓虹、蓝紫渐变、玻璃拟态、粒子、光球、宇宙背景或过度圆角。
7. 不新增在线支付、借贷、充值、会员、广告、协作或未确认的智能功能。

## 10. 可直接粘贴给 Stitch 的总提示词

```text
请为微信小程序“龙虾生意账”设计一套完整、高保真的移动端 UI，基准尺寸为 375 x 812 px。

产品是面向个体经营者的开单与客户往来账工具。核心是统一客户、多商品账单、用户自定义商品库和分类、客户回款、客户账本、生成并分享只读结账信息。它不是营销官网、金融产品或桌面后台。

视觉风格必须是未来经营控制台：石墨黑偏青背景 #0B1617、深色表面 #122526、主青绿 #00A9A5、冷青高光 #5DE6DC、出货珊瑚红 #FF6246、进货成本金色 #E7B447、成功 #4BCE91、风险 #FF6868。使用系统无衬线字体和高对比等宽金额数字，圆角 4 至 8px，精细分割线、克制阴影。标题区与图表可使用极弱点阵/网格刻度。不要紫色霓虹、游戏 HUD、粒子、宇宙背景、玻璃拟态或大面积渐变。

底部固定五个 Tab：开单、总账本、客户、查询统计、我的。

必须设计以下页面：
1. 开单：交易日期、客户选择、进货/出货方向、多商品明细表（商品、数量、单价、小计、移除）、总金额、备注、添加商品、商品库、清空、确认。
2. 商品选择弹窗：搜索、分类、商品列表、选中数量、确认加入、新增商品。
3. 商品库和商品编辑：用户自定义商品、分类、单位、默认单价、备注；无固定品种。
4. 总账本：按年月和日期分组账单、展开商品明细、查看、修改、删除。
5. 客户管理和客户编辑：搜索、添加、进货/出货筛选、当前余额、客户名称、电话、会计起始日。
6. 客户账本：日期范围、账单总额、回款总额、待结余额、账单列表、分享、生成结账信息、添加回款。
7. 客户回款：选择客户、日期、金额、回款前后余额、回款记录、提交、只读回款单。
8. 只读结账分享页：客户、账期、账单明细、账单总额、回款总额、待结余额；不可编辑、删除或支付。
9. 交易流水查询：进出货、客户、商品、日期、价格筛选，统计数量与金额，分页和查看详情。
10. 经营统计：进货、出货、回款、待结余额、趋势、商品排行、客户余额排行。
11. 我的：店铺名称、联系电话、商品库、分类管理、帮助、退出登录；不包括协作、充值或会员。

一张账单支持多商品。商品与分类都由用户维护；开单从商品库选择商品。客户回款与结账分享是关键流程。结账信息仅生成和分享，不自动修改账单或结清状态。每个页面均需要正常、加载、空数据、错误、保存中和危险操作确认状态。
```
